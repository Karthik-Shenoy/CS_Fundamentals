{
  "name": "grpc",
  "version": "1.0.0",
  "description": "grpc examples",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "tsc -outDir ./transpiledJS ./gRPCClient.ts && node ./transpiledJS/gRPCClient.js",
    "server": "tsc -outDir ./transpiledJS ./gRPCServer.ts && node ./transpiledJS/gRPCServer.js"
  },
  "keywords": [
    "gRPC"
  ],
  "author": "Karthik SHenoy",
  "license": "ISC",
  "dependencies": {
    "@grpc/grpc-js": "^1.9.5",
    "@grpc/proto-loader": "^0.7.10"
  },
  "devDependencies": {
    "@types/node": "^20.8.3"
  }
}
